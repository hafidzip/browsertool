[project]
name = "browsertool"
version = "0.1.0"
description = "Control browser webviews via Python Playwright CDP. Perform clicks, navigation, screenshots, and JS evaluation on temporary Tauri webview."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openchadpy>=0.1.23",
    "playwright>=1.61.0",
]
